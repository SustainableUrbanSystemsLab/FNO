[project]
name = "fno-mag"
version = "0.1.0"
description = "FNO for predicting magnitude from GH CSVs"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "torch>=2.0.0",
    "numpy>=1.24",
    "pandas>=1.5",
]

[tool.uv]
dev-dependencies = []
